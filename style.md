# style.md — 카드뉴스 제작 및 검토 스펙 (3단계)

message.md에서 확정된 주제(JSON)를 받아 실제 카드뉴스(캐러셀 이미지)를 만들고,
텔레그램으로 미리보기를 보내 승인받는 단계.

---

## 1. 입력

message.md에서 넘어온 JSON:

```json
{
  "date": "{날짜}",
  "selected_topic": "{확정된 주제}",
  "hook": "{후킹 포인트}",
  "format": "{형식 제안}"
}
```

---

## 1-1. 실제 반응 좋은 게시글 레퍼런스 (#경제 상위 게시물 분석)

주환님이 직접 캡처한 실제 상위 게시물 기준 분석:

| 계정 | 좋아요 | 저장 | 스타일 요약 |
|---|---|---|---|
| @aim__ing | 1,530 | 2,125 | 어두운 배경 + 지폐 이미지 위 골드 페인트 텍스처 + 굵은 흰색/틸 헤드라인 |
| @moneygraphyworld | 2,193 | 732 | 어두운 배경 + 인물 컷아웃 콜라주 + 라벨 박스("한국은행 신현송 총재" 등) |
| @healing.zip | 31,000 | - | 흑백 대비 + 자극적 궁금증 유발 헤드라인 |

**공통 시각 패턴**:
- **배경**: 크림/화이트 계열이 아니라 **어두운(거의 검정) 배경**이 압도적으로 많음
- **헤드라인 타이포**: 굵은 산세리프(고딕), 흰색 메인 텍스트 + 포인트 컬러(틸/골드/옐로) 강조 단어 조합
- **이미지 소재**: 일러스트가 아니라 **실사 사진 콜라주** (지폐, 건물, 인물 컷아웃) 위에 텍스트 오버레이
- **라벨/콜아웃 박스**: 인물이나 요소를 짚어주는 작은 라벨 박스가 신뢰도를 높임
- **좌상단 워터마크**: 계정 로고/이름을 작게 고정 배치

**공통 카피 패턴**:
- 정보 나열형 제목보다 **궁금증 유발형 헤드라인**이 강함 — "갑자기 ~하려고 한다", "이유가 소름 돋음", "~카더라"
- **저장(bookmark) 수가 좋아요만큼, 때로는 그 이상으로 높음** — 리스트/추천형 정보가 저장을 특히 유도함

**확정된 방향**: 저금통장(크림색 장부) 컨셉은 폐기 → 어두운 배경+콜라주로 전환했다가, 최종적으로 **밝은 아이보리 배경**으로 재조정. 사진(상단)+텍스트(하단, 아이보리 배경, 다크 잉크 글씨) 구조로 확정. 신규 템플릿 파일: `finance_card_template.html`

---

## 2. 카피 작성

- 톤앤매너: 밝은 아이보리 배경 + 궁금증 유발형 헤드라인 (레퍼런스 분석 결과 반영) — 정보 나열형 제목보다 "왜 ~일까", "갑자기 ~하려는 이유" 같은 후킹형 표현 우선
- **실제 반응 데이터 기반 원칙** (벤치마크 계정 상위 게시물 분석 결과):
  - 캡션 끝에 **댓글 유도형 CTA** 포함 — 예: "궁금하면 댓글로 '방법' 남겨주세요" (댓글 수가 눈에 띄게 올라가는 패턴)
  - **좋아요·저장 요청 문구**를 캡션에 항상 명시 — 예: "저장해두면 나중에 다시 보기 편해요"
  - 여러 편으로 이어갈 수 있으면 **다음 편 예고** 문구 추가 — 예: "다음엔 OO 편으로 돌아올게요"
  - 정보 나열만 하지 말고 짧은 **공감/서사 한 줄**을 표지나 마무리에 섞기
- 카드 구성 (형식 제안에 따라 다르지만 기본 틀):
  1. 표지 카드 — 후킹 문구 (예: "이거 하나로 한 달 20만원 아꼈어요")
  2. 본문 카드 2~7장 — 리스트/체크리스트/단계별 내용 (형식 제안에 맞춰 유동적)
  3. 마무리 카드 — 요약 + CTA (팔로우, 저장 유도)
- 카드 수: 5장 (확정값. 형식상 꼭 필요하면 ±1장 정도만 유동)
- **카드 1장당 리스트 항목은 최대 4개까지** (템플릿 여백 기준 확인됨). 5개 이상 필요하면 항목을 두 카드로 나눌 것
- 문구 길이: 카드 1장당 핵심 문장 1~2개, 짧고 스캔하기 쉽게

## 3. 디자인 템플릿 적용

- **아이보리 배경 + 상단 이미지/하단 텍스트 HTML/CSS 템플릿**에 카피를 자동 삽입
- 템플릿 파일: `finance_card_template.html` (5카드 구성 — 표지 1장, 내지 3장, 마무리 1장)

### 3-0. 매일 포인트 컬러 교체 (템플릿이 매번 똑같아 보이지 않도록)
아이보리 배경 + 다크 잉크 텍스트는 항상 고정하고, 포인트 컬러(`--teal`, `--gold`)만 아래 팔레트 중 하나를 매일 돌아가며 사용한다. **어제 쓴 팔레트와 같은 걸 연속으로 쓰지 않는다.**

| 팔레트 | --teal | --gold | 분위기 |
|---|---|---|---|
| A. 틸 + 골드 (기본) | `#0F7A6C` | `#A8721E` | 차분한 신뢰감 |
| B. 네이비 + 테라코타 | `#1F3A5F` | `#B85C38` | 묵직하고 안정적 |
| C. 세이지그린 + 러스트 | `#4A6741` | `#A84B32` | 따뜻하고 자연스러움 |
| D. 블루 + 앰버 | `#1D5A8A` | `#B8860B` | 선명하고 산뜻함 |

- 적용 방법: `finance_card_template.html`의 `:root` 블록에서 `--teal`, `--gold` 값만 그날 선택한 팔레트로 교체 (다른 색상 변수 `--bg`, `--ink`, `--ink-soft`는 건드리지 않음)
- 선택 기준: 순서대로 요일 로테이션(A→B→C→D→A...) 하거나, 그날 주제 톤에 맞춰 자유롭게 선택 — 단, 직전 실행과 겹치지 않게
- 직전 팔레트 확인용: 저장소에 `last_palette.txt` 파일 하나를 두고, 실행할 때마다 그 안의 값을 읽어 다른 팔레트를 고른 뒤 새 값으로 덮어쓴다

- 현재는 예시 주제("통장 쪼개기") 텍스트가 하드코딩되어 있음. 실제 자동화 시엔 아래를 매일 교체해야 함:
  - `.headline`/`.accent` (표지 헤드라인), `.heading` (각 페이지 소제목), `.t`/`.d` (항목별 제목/본문), `.callout` (강조 수치 박스), `.stat`/`.cta` (마무리 요약)
  - 교체 방식: 각 요소에 고유 id를 부여해 스크립트에서 텍스트만 바꿔치기하거나, 이 HTML을 Jinja2/문자열 템플릿으로 변환해 변수 주입
- 카드별로 템플릿에 텍스트 변수를 채워 넣어 카드 수만큼 HTML 파일 생성
- **폰트 규칙**: 한글이 섞인 텍스트는 반드시 Noto Sans KR 사용 (letter-spacing 최소화, 0.5px 이하). IBM Plex Mono는 순수 영문·숫자(ENTRY 01, P.01 같은)에만 사용 — 한글에 모노스페이스+넓은 자간을 적용하면 글자 사이가 부자연스럽게 벌어짐
- **레이아웃**: 카드마다 상단 46%는 이미지, 하단은 텍스트(밝은 아이보리 배경, 다크 잉크 글씨) — 5장 전부 동일 구조
- **이미지 소스 (매일 자동 준비, 고정 아님)**: 카드별로 그날 확정된 주제/내용에 맞춰 매번 새로 준비해서 교체한다. 아래 두 방식 중 하나를 쓴다.

  ### 방식 A. 직접 제작 (AI 이미지 생성) — 우선 시도
  - 프로젝트에 이미지 생성 도구/스킬이 연결되어 있으면(예: `/mnt/skills/user/imagegen/SKILL.md` 또는 Nanobanana 등 연결된 이미지 생성 API) 이걸 우선 사용
  - 카드 5장 각각의 핵심 키워드로 생성 프롬프트 작성 — 톤은 이 템플릿의 분위기(밝은 아이보리 배경과 어울리는 깔끔한 사진/일러스트 느낌, 과한 채도·텍스트 삽입 금지)에 맞춤
  - 예: "재테크 통장 정리, 미니멀한 사물 사진 스타일, 따뜻한 아이보리 톤 배경, 텍스트 없음"
  - 생성된 이미지를 카드 폴더에 저장 후 템플릿 `<img src="...">`에 로컬 경로로 삽입
  - 실제 인물(공직자, 유명인 등)을 생성하지 않음 — 사물/추상적 장면 위주

  ### 방식 B. 스톡 사진 검색 — 생성 도구가 없거나 실패 시 대체
  1. 카드 5장 각각에 대해 핵심 키워드 1개 추출 (예: 표지=주제 후킹 키워드, Entry 01~03=각 카드 소제목 키워드, 마무리=요약 키워드)
  2. 키워드마다 `web_search`로 `"unsplash {키워드} photo"` 검색 → 적절한 결과 URL 선택
  3. `web_fetch`로 그 Unsplash 페이지를 열어서 `images.unsplash.com/photo-...` 직링크 확인 + 페이지에 "Free to use under the Unsplash License" 문구가 있는지 확인 (Unsplash+ 전용/유료 표시가 있으면 다른 사진으로)
  4. 실제 인물(공직자, 유명인 등)이 나오는 사진은 초상권 문제로 제외 — 사물/사물 중심 사진만 사용
  5. 확인된 직링크를 finance_card_template.html의 해당 카드 `<img src="...">`에 그대로 교체

  ### 공통 fallback (이미지 재사용 금지)
  - 방식 A(생성)가 안 되면 방식 B(검색)로, 방식 B에서도 처음 검색어가 안 맞으면 **키워드를 바꿔서 재검색** (같은 사진을 어제와 오늘 반복해서 쓰지 않는다)
  - 여러 번 검색해도 도저히 못 찾을 경우에만, 카드 주제의 상위 카테고리(예: 세부 키워드 대신 "재테크", "저축" 같은 넓은 키워드)로 한 번 더 검색해서 그걸 사용 — 이전에 썼던 특정 사진을 그대로 재사용하지 않는다

## 4. 이미지 렌더링

- HTML → 이미지 변환: Puppeteer 또는 Playwright로 각 카드 HTML을 스크린샷
  - 예약 작업 환경은 root 권한으로 실행되어 Chromium이 기본 설정으로는 실행 안 됨 → `puppeteer.launch({ args: ['--no-sandbox'] })` 필요 (실제 테스트로 확인됨)
- 규격: 1080x1350px (4:5, 세로형) — 확정값. 정사각형보다 피드에서 더 많은 화면을 차지해 노출에 유리
- 파일명 규칙: `{날짜}_card_01.png`, `{날짜}_card_02.png` ...

## 5. 자체 검수 (Self-QA, 사람 개입 없음)

사람에게 보내서 승인받는 단계가 아니라, 게시 전 Claude가 스스로 아래 항목을 점검하는 단계.

### 5-1. 카피 단계 검수 (렌더링 전)
- 오탈자/맞춤법 확인
- 카드별 문구 길이가 템플릿 텍스트 박스에 넘치지 않는지 (글자 수 기준 사전 체크)
- 숫자·통계 인용 시 앞 단계(search.md) 근거와 실제로 일치하는지 (없는 수치 지어내지 않았는지)

### 5-2. 렌더링 후 이미지 검수 (완성된 스크린샷을 다시 확인)
각 카드 이미지를 하나씩 열어서 아래를 확인:
- **텍스트 잘림/오버플로우**: 글자가 카드 박스 밖으로 나가거나 잘렸는지
- **placeholder 미치환 흔적**: `{변수명}` 같은 템플릿 문법이 그대로 남아있는지
- **이미지 누락/깨짐**: 배경·아이콘 등이 정상 표시되는지, 빈 사각형이나 깨진 이미지 아이콘이 있는지
- **카드 순서 정합성**: 표지 → 본문 → 마무리 순서가 맞는지, 번호가 있다면 순서대로인지
- **규격 확인**: 모든 카드가 동일한 해상도/비율인지

### 5-3. 문제 발견 시
- 문제 있는 카드만 다시 생성 (전체 재생성 아님)
- 재검수 → 통과할 때까지 반복 (최대 3회, 3회 넘게 실패하면 사람에게 알리고 중단)

### 5-4. 통과 시
- 4단계(인스타 게시)로 자동 진행
- 선택: 게시 직전 "오늘 카드뉴스 검수 통과, 게시 진행합니다" 정도의 **참고용 알림만** 텔레그램으로 보낼 수 있음 (승인 대기 아님, 정보성 통지)

## 6. 출력 (4단계로 전달)

```json
{
  "date": "{날짜}",
  "qa_passed": true,
  "qa_retries": 0,
  "image_paths": [
    "{경로}/{날짜}_card_01.png",
    "{경로}/{날짜}_card_02.png"
  ],
  "caption": "{인스타 게시글 본문 캡션 — 댓글 유도 CTA + 저장 요청 문구 포함 + 해시태그}"
}
```

---

## 7. 확정된 사항
- 이미지 규격: 1080x1350px (4:5)
- 카드 수: 5장
- 뉴스/트렌드 근거는 search.md 산출물 그대로 사용, 벤치마크 계정 3개 반영

## 8. 남은 실행 전 조치 (본인 확인 필요)
- Puppeteer/Playwright 실행 환경(Node.js) 이 Claude Code 예약 작업 환경에서 동작하는지 테스트 필요
- 자체 검수(5-2)는 렌더링된 이미지를 시각적으로 다시 확인하는 단계라, 예약 작업 환경이 이미지 파일을 읽을 수 있는지 확인 필요
- 캡션 고정 해시태그 세트 정하기 (예: #재테크 #짠테크 #절약)
- 템플릿에 실제 텍스트를 주입하는 스크립트(치환 로직) 작성 필요 — 지금은 수동으로 예시 텍스트만 들어있는 상태
- 이미지 생성 도구(방식 A)가 프로젝트에 연결되어 있는지 확인 필요 — 없으면 자동으로 방식 B(스톡 사진 검색)로만 동작
